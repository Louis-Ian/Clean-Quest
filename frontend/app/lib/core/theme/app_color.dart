import 'package:flutter/material.dart';

class AppColors {
  // Buttons
  static const buttonPrimary = Color(0xFF23A9F2);
  static const buttonSecondary = Color(0xFFF0F0F0);
  static const buttonDisabled = Color(0xFFE0E0E0);

  // Text
  static const textPrimary = Colors.black;
  static const textPrimaryMuted = Colors.black54;
  static const textSecondary = Color(0xFF666666);
  static const textDisabled = Color(0xFFB0B0B0);

  // Backgrounds
  static const background = Colors.white;
}
